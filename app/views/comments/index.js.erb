<% comments_html = render :partial => 'comments', :locals => {:comments => @comments, :test => @test, :project => @project } %>

$("#<%= dom_id(@test) %> .comments").empty().append($("<%= escape_javascript(comments_html) %>"))